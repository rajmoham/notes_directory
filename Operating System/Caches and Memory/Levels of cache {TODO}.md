runtime and memory
how OS manages what goes into each level of cache
    what are predictions based on?

Ram latency checking

what is cache associative

physical and virtual addresses

cache misses
- tld misses (when the program first starts) - compulsory miss
- capacity miss
- conflict miss

switching between threads but managing process states
- flush tld (bad)
- tag entries for processes and swap out tags

cache affinity